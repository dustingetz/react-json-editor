(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        define([
            'lodash',
            'react',
            'wingspan-cursor',
            'react-treeview'
        ], factory);
    } else {
        root.JsonEditor = factory(root._, root.React, root.WingspanCursor, root.TreeView);
    }
}(this, function (_, React, WingspanCursor, TreeView) {
