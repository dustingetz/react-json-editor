(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        define([
            'lodash',
            'react',
            'react-cursor'
        ], factory);
    } else {
        root.JsonEditor = factory(root._, root.React, root.ReactCursor);
    }
}(this, function (_, React, ReactCursor) {
